# d.ground environment

# Supabase — shared with d.connect family
NEXT_PUBLIC_SUPABASE_URL="https://yvasxiixcnukyzxlnqvb.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="<paste anon key from Supabase Dashboard → Project Settings → API>"

# Server-only (DO NOT expose to client)
SUPABASE_SERVICE_ROLE_KEY="<paste service_role key from same page — keep secret>"

# Super admin bootstrap — comma-separated emails get auto-promoted on first login
SUPER_ADMINS="ianbhak@gmail.com"

# LLM (W3+)
ANTHROPIC_API_KEY=""
VOYAGE_API_KEY=""

# Email (W4+)
RESEND_API_KEY=""

# App URL (used for redirects)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
